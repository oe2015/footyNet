<!DOCTYPE html>
<html>
  <head>
    <title>FootyNet - Profile</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <div class="profile">
      <h1>Profile</h1>
      <h2>Username:</h2>
      <p>{{user.username}}</p>
      <h2>Email:</h2>
      <p>{{user.email}}</p>
      {{#if user.team}}
        <h2>Team:</h2>
        <p><a href="/team/{{user.team._id}}">{{user.team.name}}</a></p>
      {{else}}
        <p>You are not part of a team.</p>
      {{/if}}
    </div>
  </body>
</html>
